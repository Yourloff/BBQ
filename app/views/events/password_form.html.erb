<h1>Доступ только по пин-коду</h1>

<%= form_tag('', method: 'post') do |f| %>
  <div class="form-group">
    <%= f.label 'Пин-код', :pincode %>
    <%= f.text_field_tag :pincode, params[:pincode], class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit_tag %>
  </div>
<% end %>

<%= link_to 'На главную', events_path %>
